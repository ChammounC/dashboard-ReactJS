const MapChannels = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Map Channels
    </div>
  );
};

export default MapChannels;
